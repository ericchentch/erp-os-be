{
  "devDependencies": {
    "localtunnel": "^2.0.2",
    "husky": "^8.0.3",
    "prettier": "^2.8.4",
    "prettier-plugin-java": "^2.0.0"
  },
  "scripts": {
    "dev": " mvn spring-boot:run",
    "local-deploy": "lt --port 8080 --subdomain erp-os-be",
    "prepare": "husky install",
    "check-format": "mvn prettier:check ",
    "format": "mvn prettier:write "
  }
}